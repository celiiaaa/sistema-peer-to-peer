/*
 * This is sample code generated by rpcgen.
 * These are only templates and you can use them
 * as a guideline for developing your own functions.
 */

#include "operacion.h"


void
imprimir_operacion_1(char *host)
{
	CLIENT *clnt;
	enum clnt_stat retval_1;
	void *result_1;
	struct Operacion print_operacion_1_arg1;

#ifndef	DEBUG
	clnt = clnt_create (host, IMPRIMIR_OPERACION, IMPRIMIR_OPERACION_V1, "udp");
	if (clnt == NULL) {
		clnt_pcreateerror (host);
		exit (1);
	}
#endif	/* DEBUG */

	retval_1 = print_operacion_1(print_operacion_1_arg1, &result_1, clnt);
	if (retval_1 != RPC_SUCCESS) {
		clnt_perror (clnt, "call failed");
	}
#ifndef	DEBUG
	clnt_destroy (clnt);
#endif	 /* DEBUG */
}


int
main (int argc, char *argv[])
{
	char *host;

	if (argc < 2) {
		printf ("usage: %s server_host\n", argv[0]);
		exit (1);
	}
	host = argv[1];
	imprimir_operacion_1 (host);
exit (0);
}
